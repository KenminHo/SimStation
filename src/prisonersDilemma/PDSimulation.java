/*
package prisonersDilemma;
import simstation.*;
import mvc.*;

public class PDSimulation extends Simulation {
    @Override
    public void populate() {
        for(int i = 0; i < 15; i++)
            addAgent(new Prisoner());
    }
    @Override public void stats() {
        //The statistics box should show the average fitness for each strategy.
        Utilities.inform(new String[]{"[PLACEHOLDER] " +
                "\nThe statistics box should show the average fitness for each strategy."});
    }

    public static void main(String[] args) {
        AppPanel panel = new SimulationPanel(new PDFactory());
        panel.display();
    }
}
*/
